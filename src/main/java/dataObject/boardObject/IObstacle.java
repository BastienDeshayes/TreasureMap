package dataObject.boardObject;

/**
 * This interface represent obstacle to Moving entities (Can be used later to define different type of obstacle (ex Lake River, Tree)
  */
public interface IObstacle extends IBoardEntity {
}
